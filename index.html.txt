<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Decision Matrix Pro | مصفوفة القرار الذكية</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --secondary: #00cec9;
            --danger: #ff7675;
            --glass: rgba(255, 255, 255, 0.1);
            --text: #ffffff;
        }

        * { box-sizing: border-box; transition: all 0.3s ease; }

        body {
            font-family: 'Cairo', sans-serif;
            background: linear-gradient(45deg, #0f0c29, #302b63, #24243e);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            color: var(--text);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
            padding: 20px;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .container {
            background: var(--glass);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 25px;
            padding: 40px;
            width: 100%;
            max-width: 650px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.3);
        }

        h1 { text-align: center; font-size: 2rem; margin-bottom: 10px; color: var(--secondary); }
        .subtitle { text-align: center; opacity: 0.8; margin-bottom: 30px; font-size: 0.9rem; }

        .input-section {
            display: flex;
            gap: 15px;
            margin-bottom: 30px;
        }

        input[type="text"] {
            flex: 1;
            padding: 15px;
            border-radius: 12px;
            border: 1px solid rgba(255,255,255,0.2);
            background: rgba(0,0,0,0.2);
            color: white;
            font-size: 1rem;
            text-align: center;
        }

        input:focus { border-color: var(--secondary); outline: none; background: rgba(0,0,0,0.4); }

        .criteria-card {
            background: rgba(255,255,255,0.05);
            padding: 20px;
            border-radius: 15px;
            margin-bottom: 15px;
            border-right: 4px solid var(--primary);
        }

        .criteria-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        select {
            padding: 8px 15px;
            border-radius: 8px;
            background: #2d3436;
            color: white;
            border: none;
            cursor: pointer;
        }

        .weight-box {
            margin-top: 10px;
        }

        .weight-label {
            display: flex;
            justify-content: space-between;
            font-size: 0.8rem;
            margin-bottom: 5px;
            color: var(--secondary);
        }

        input[type="range"] {
            width: 100%;
            height: 5px;
            border-radius: 5px;
            accent-color: var(--secondary);
        }

        .decide-btn {
            width: 100%;
            padding: 18px;
            margin-top: 20px;
            border-radius: 15px;
            border: none;
            background: linear-gradient(90deg, var(--primary), #a29bfe);
            color: white;
            font-weight: bold;
            font-size: 1.2rem;
            cursor: pointer;
            box-shadow: 0 10px 20px rgba(108, 92, 231, 0.3);
        }

        .decide-btn:hover { transform: translateY(-3px); box-shadow: 0 15px 25px rgba(108, 92, 231, 0.5); }
        #resultArea {
            margin-top: 30px;
            display: none;
            padding: 25px;
            border-radius: 15px;
            background: rgba(255,255,255,0.1);
            animation: slideUp 0.5s ease-out;
        }

        @keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

        .winner-tag {
            font-size: 1.5rem;
            color: var(--secondary);
            font-weight: bold;
            display: block;
            margin-bottom: 10px;
        }

        .analysis-text { font-size: 0.95rem; line-height: 1.6; color: #dfe6e9; }
        
        .badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.75rem;
            margin-left: 5px;
            background: var(--primary);
        }
    </style>
</head>
<body>

<div class="container">
    <h1>🧠 Decision Matrix Pro</h1>
    <p class="subtitle">قم بمقارنة الخيارات بناءً على أولوياتك الخاصة</p>

    <div class="input-section">
        <input type="text" id="op1" placeholder="الخيار الأول">
        <input type="text" id="op2" placeholder="الخيار الثاني">
    </div>

    <div class="criteria-card">
        <div class="criteria-header">
            <span>💰 الميزانية والتكلفة</span>
            <select id="v_price">
                <option value="1">الخيار الأول أفضل</option>
                <option value="2">الخيار الثاني أفضل</option>
            </select>
        </div>
        <div class="weight-box">
            <div class="weight-label"><span>أهمية السعر بالنسبة لك</span> <span id="w_price_val">50%</span></div>
            <input type="range" id="w_price" min="1" max="100" value="50" oninput="updateVal('w_price')">
        </div>
    </div>

    <div class="criteria-card" style="border-right-color: #fdcb6e;">
        <div class="criteria-header">
            <span>⏱️ توفير الوقت والجهد</span>
            <select id="v_time">
                <option value="1">الخيار الأول أفضل</option>
                <option value="2">الخيار الثاني أفضل</option>
            </select>
        </div>
        <div class="weight-box">
            <div class="weight-label"><span>مدى استعجالك للقرار</span> <span id="w_time_val">50%</span></div>
            <input type="range" id="w_time" min="1" max="100" value="50" oninput="updateVal('w_time')">
        </div>
    </div>

    <div class="criteria-card" style="border-right-color: #ff7675;">
        <div class="criteria-header">
            <span>😊 الرضا النفسي والسعادة</span>
            <select id="v_happy">
                <option value="1">الخيار الأول أفضل</option>
                <option value="2">الخيار الثاني أفضل</option>
            </select>
        </div>
        <div class="weight-box">
            <div class="weight-label"><span>أهمية شعورك الداخلي</span> <span id="w_happy_val">50%</span></div>
            <input type="range" id="w_happy" min="1" max="100" value="50" oninput="updateVal('w_happy')">
        </div>
    </div>

    <button class="decide-btn" onclick="calculateDecision()">🎯 اتخاذ القرار النهائي</button>

    <div id="resultArea">
        <span class="badge">النتيجة النهائية</span>
        <span id="winnerName" class="winner-tag"></span>
        <div id="analysis" class="analysis-text"></div>
    </div>
</div>

<script>
    function updateVal(id) {
        document.getElementById(id + '_val').innerText = document.getElementById(id).value + '%';
    }

    function calculateDecision() {
        const op1 = document.getElementById("op1").value || "الخيار الأول";
        const op2 = document.getElementById("op2").value || "الخيار الثاني";

        // المعطيات (1 للخيارة الأول، 2 للثاني) والأوزان
        const data = [
            { winner: document.getElementById("v_price").value, weight: parseInt(document.getElementById("w_price").value), label: "التكلفة" },